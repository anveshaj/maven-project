pipeline {
    agent any
	stage {
	    stage ('pacakaging') {
	        steps {
		       ls /bin > /opt/file55
			    }
		}		
		stage ('deploying') {
			steps {
			    ls /var > /opt/file66
				}
		}
	}
}
	
